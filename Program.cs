/*
Purpose:
  - Program that creates an NPC by randomly combining different traits and attributes
      - species
      - positive traits
      - negative traits
      (- gender)
      (- age)

How:
  - read attributes from files (later database)
  - randomly pick a set number from each and combine them
  - formatted output to console
  - write output to file
*/

// Load all File Content

// Generate first default Entry

// Display Menu
/*
    - Generate next NPC
    - Save NPC
    - Exit
*/

// NextNPC()

// SaveNPC()

// Exit()

using npc_generator;

Generator generator = new();
generator.Run();